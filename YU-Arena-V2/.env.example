# YU Arena — Environment Variables
# On Cloud Run, set these via --set-env-vars or Secret Manager

# Server port (Cloud Run sets PORT=8080 automatically)
PORT=8080

# ─── PostgreSQL (Cloud SQL) ──────────────────────────
# For Cloud SQL via private IP or Auth Proxy:
DB_HOST=/cloudsql/PROJECT:REGION:INSTANCE
DB_PORT=5432
DB_NAME=yu_arena
DB_USER=yu_app
DB_PASSWORD=change-me
DB_SSL=false
# Set DB_SSL=true if connecting over public IP

# ─── Operator Auth ───────────────────────────────────
JWT_SECRET=change-me-in-production
JWT_EXPIRY_SECONDS=86400
DEMO_ACCESS_CODE=demo1234

# ─── WhatsApp (Twilio) ──────────────────────────────
# Required for rush list members to receive WhatsApp when you launch a drop.
# Recipients must join the Twilio WhatsApp sandbox first (send "join <code>" to sandbox number).
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=+1XXXXXXXXXX
BASE_URL=https://yu-arena-381932264033.us-east1.run.app

# ─── Legacy Agent Keys ──────────────────────────────
SCOUT_API_KEY=yu-scout-key-demo
CLOSER_API_KEY=yu-closer-key-demo
ADMIN_API_KEY=

# ─── Node ────────────────────────────────────────────
NODE_ENV=production
